# Client (Vercel) environment variables — copy to .env.local locally
# Full backend API base URL including the /api suffix.
# On Vercel set this to your Render backend URL, e.g.:
#   https://loanflow-api.onrender.com/api
NEXT_PUBLIC_API_URL=http://localhost:5000/api